import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: "#fff",
        alignItems: "center",
        justifyContent: "center"
    },
    text_field: {
        width: 200,
        height: 40,
        borderColor: '#bfbfbf',
        borderWidth: 1,
        padding: 10,
        marginBottom: 10,
    }
});